#!/usr/bin/env bash
# Everything is done by the Features named in 'dependsOn'; nothing to install here.
set -euo pipefail
echo "[claude-code-rtk] claude-code + rtk installed via Feature dependencies."
