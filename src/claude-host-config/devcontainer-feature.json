{
	"id": "claude-host-config",
	"version": "1.0.0",
	"name": "Claude Code host config",
	"description": "Bind-mounts the host's ~/.claude and ~/.claude.json into the container and links them into the remote user's home, so Claude Code and rtk run with your own credentials, memory, hooks, plugins and history. Meant to be injected personally through the 'dev.containers.defaultFeatures' setting rather than committed to a shared devcontainer.json.",
	"documentationURL": "https://github.com/khannurien/devcontainer-features/tree/main/src/claude-host-config",
	"mounts": [
		{
			"source": "${localEnv:HOME}/.claude",
			"target": "/claude-host/claude",
			"type": "bind"
		},
		{
			"source": "${localEnv:HOME}/.claude.json",
			"target": "/claude-host/claude.json",
			"type": "bind"
		}
	],
	"installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
