{
	"id": "opencode-host-config",
	"version": "1.1.0",
	"name": "opencode host config",
	"description": "Bind-mounts the host's ~/.config/opencode, ~/.local/state/opencode and ~/.local/share/opencode into the container and links them into the remote user's home, so opencode runs with your own providers, plugins, selected model, prompt history and session history. Meant to be injected personally through the 'dev.containers.defaultFeatures' setting rather than committed to a shared devcontainer.json.",
	"documentationURL": "https://github.com/khannurien/devcontainer-features/tree/main/src/opencode-host-config",
	"mounts": [
		{
			"source": "${localEnv:HOME}/.config/opencode",
			"target": "/opencode-host/config",
			"type": "bind"
		},
		{
			"source": "${localEnv:HOME}/.local/state/opencode",
			"target": "/opencode-host/state",
			"type": "bind"
		},
		{
			"source": "${localEnv:HOME}/.local/share/opencode",
			"target": "/opencode-host/data",
			"type": "bind"
		}
	],
	"installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
