{
	"id": "opencode",
	"version": "1.1.0",
	"name": "opencode",
	"description": "Installs the opencode AI coding agent CLI for the remote user. Uses opencode's own $HOME layout, so 'opencode upgrade' works inside the container instead of stopping on an ownership prompt or leaving a second, stale copy behind.",
	"documentationURL": "https://github.com/khannurien/devcontainer-features/tree/main/src/opencode",
	"options": {
		"opencodeVersion": {
			"type": "string",
			"proposals": ["latest"],
			"default": "latest",
			"description": "opencode version to install ('latest' or a pinned version such as 1.17.9)."
		}
	},
	"installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
